_schema-version: 3.3.0
ID: lockbox-template-builder
description: A simple CAP project.
version: 1.0.0
modules:
- name: lockbox-template-builder-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: lockbox-template-builder-postgres
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: lockbox-template-builder-postgres-deployer
  type: nodejs
  path: gen/pg
  requires:
  - name: lockbox-template-builder-postgres
  parameters:
    buildpack: nodejs_buildpack
    no-route: true
    no-start: true
    tasks:
    - command: npm start
      name: deploy-to-postgresql
- name: lockbox-template-builder-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: lockbox-template-builder-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - navtemplatebuildertemplateuimap.zip
      name: navtemplatebuildertemplateuimap
      target-path: resources/
- name: navtemplatebuildertemplateuimap
  type: html5
  path: app/templateuimap
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: lockbox-template-builder-postgres
  type: org.cloudfoundry.existing-service
  parameters:
    service: postgresql-db
    service-name: test-Postgre
    service-plan: trial
- name: lockbox-template-builder-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: lockbox-template-builder-html5-service
    service-plan: app-host
- name: lockbox-template-builder-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: lockbox-template-builder-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
